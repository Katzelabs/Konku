// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: reviewsets.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const addSetCard = `-- name: AddSetCard :exec
INSERT INTO review_set_cards (set_id, user_id, card_id, position)
VALUES ($1, $2, $3, $4)
`

type AddSetCardParams struct {
	SetID    uuid.UUID `json:"set_id"`
	UserID   uuid.UUID `json:"user_id"`
	CardID   uuid.UUID `json:"card_id"`
	Position int32     `json:"position"`
}

func (q *Queries) AddSetCard(ctx context.Context, arg AddSetCardParams) error {
	_, err := q.db.Exec(ctx, addSetCard,
		arg.SetID,
		arg.UserID,
		arg.CardID,
		arg.Position,
	)
	return err
}

const addSetCategory = `-- name: AddSetCategory :exec
INSERT INTO review_set_categories (user_id, set_id, category_id)
VALUES ($1, $2, $3)
ON CONFLICT DO NOTHING
`

type AddSetCategoryParams struct {
	UserID     uuid.UUID `json:"user_id"`
	SetID      uuid.UUID `json:"set_id"`
	CategoryID uuid.UUID `json:"category_id"`
}

func (q *Queries) AddSetCategory(ctx context.Context, arg AddSetCategoryParams) error {
	_, err := q.db.Exec(ctx, addSetCategory, arg.UserID, arg.SetID, arg.CategoryID)
	return err
}

const addSetDomain = `-- name: AddSetDomain :exec
INSERT INTO review_set_domains (user_id, set_id, domain_id)
VALUES ($1, $2, $3)
ON CONFLICT DO NOTHING
`

type AddSetDomainParams struct {
	UserID   uuid.UUID `json:"user_id"`
	SetID    uuid.UUID `json:"set_id"`
	DomainID uuid.UUID `json:"domain_id"`
}

func (q *Queries) AddSetDomain(ctx context.Context, arg AddSetDomainParams) error {
	_, err := q.db.Exec(ctx, addSetDomain, arg.UserID, arg.SetID, arg.DomainID)
	return err
}

const archiveReviewSet = `-- name: ArchiveReviewSet :one
UPDATE review_sets
SET archived_at = now(), updated_at = now()
WHERE id = $1 AND user_id = $2 AND archived_at IS NULL
RETURNING id, user_id, title, description, selection, question_count, time_limit_minutes, archived_at, created_at, updated_at, format
`

type ArchiveReviewSetParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

// The normal way to retire a set. Deleting one that has runs would destroy its
// score history while the individual answers survive in review_logs, which is
// the worst of both (D-051).
func (q *Queries) ArchiveReviewSet(ctx context.Context, arg ArchiveReviewSetParams) (ReviewSet, error) {
	row := q.db.QueryRow(ctx, archiveReviewSet, arg.ID, arg.UserID)
	var i ReviewSet
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.Selection,
		&i.QuestionCount,
		&i.TimeLimitMinutes,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Format,
	)
	return i, err
}

const clearSetCards = `-- name: ClearSetCards :exec
DELETE FROM review_set_cards
WHERE set_id = $1 AND user_id = $2
`

type ClearSetCardsParams struct {
	SetID  uuid.UUID `json:"set_id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) ClearSetCards(ctx context.Context, arg ClearSetCardsParams) error {
	_, err := q.db.Exec(ctx, clearSetCards, arg.SetID, arg.UserID)
	return err
}

const clearSetCategories = `-- name: ClearSetCategories :exec
DELETE FROM review_set_categories WHERE set_id = $1 AND user_id = $2
`

type ClearSetCategoriesParams struct {
	SetID  uuid.UUID `json:"set_id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) ClearSetCategories(ctx context.Context, arg ClearSetCategoriesParams) error {
	_, err := q.db.Exec(ctx, clearSetCategories, arg.SetID, arg.UserID)
	return err
}

const clearSetDomains = `-- name: ClearSetDomains :exec

DELETE FROM review_set_domains WHERE set_id = $1 AND user_id = $2
`

type ClearSetDomainsParams struct {
	SetID  uuid.UUID `json:"set_id"`
	UserID uuid.UUID `json:"user_id"`
}

// The filters. Empty means unfiltered, which is what a NULL domain_id meant
// before 00012 split one nullable column into two join tables.
func (q *Queries) ClearSetDomains(ctx context.Context, arg ClearSetDomainsParams) error {
	_, err := q.db.Exec(ctx, clearSetDomains, arg.SetID, arg.UserID)
	return err
}

const createReviewSet = `-- name: CreateReviewSet :one
INSERT INTO review_sets (user_id, title, description, selection,
                         question_count, time_limit_minutes, format)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, user_id, title, description, selection, question_count, time_limit_minutes, archived_at, created_at, updated_at, format
`

type CreateReviewSetParams struct {
	UserID           uuid.UUID `json:"user_id"`
	Title            string    `json:"title"`
	Description      string    `json:"description"`
	Selection        string    `json:"selection"`
	QuestionCount    *int32    `json:"question_count"`
	TimeLimitMinutes *int32    `json:"time_limit_minutes"`
	Format           string    `json:"format"`
}

func (q *Queries) CreateReviewSet(ctx context.Context, arg CreateReviewSetParams) (ReviewSet, error) {
	row := q.db.QueryRow(ctx, createReviewSet,
		arg.UserID,
		arg.Title,
		arg.Description,
		arg.Selection,
		arg.QuestionCount,
		arg.TimeLimitMinutes,
		arg.Format,
	)
	var i ReviewSet
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.Selection,
		&i.QuestionCount,
		&i.TimeLimitMinutes,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Format,
	)
	return i, err
}

const createRun = `-- name: CreateRun :one
INSERT INTO review_runs (set_id, user_id, run_date)
VALUES ($1, $2, $3::date)
RETURNING id, set_id, user_id, started_at, finished_at, run_date, total_count, correct_count
`

type CreateRunParams struct {
	SetID   uuid.UUID `json:"set_id"`
	UserID  uuid.UUID `json:"user_id"`
	RunDate time.Time `json:"run_date"`
}

// run_date is the client's LOCAL YYYY-MM-DD, passed in rather than derived
// from now(), for the same reason as focus_sessions.session_date: a run at
// 23:00 belongs to that day and the server may be in another timezone.
func (q *Queries) CreateRun(ctx context.Context, arg CreateRunParams) (ReviewRun, error) {
	row := q.db.QueryRow(ctx, createRun, arg.SetID, arg.UserID, arg.RunDate)
	var i ReviewRun
	err := row.Scan(
		&i.ID,
		&i.SetID,
		&i.UserID,
		&i.StartedAt,
		&i.FinishedAt,
		&i.RunDate,
		&i.TotalCount,
		&i.CorrectCount,
	)
	return i, err
}

const deleteReviewSet = `-- name: DeleteReviewSet :execrows
DELETE FROM review_sets
WHERE id = $1 AND user_id = $2
`

type DeleteReviewSetParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

// Only succeeds when nothing references it: review_runs is ON DELETE NO
// ACTION, so a set that has ever been run raises foreign_key_violation and the
// handler answers 409.
func (q *Queries) DeleteReviewSet(ctx context.Context, arg DeleteReviewSetParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteReviewSet, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteRun = `-- name: DeleteRun :execrows
DELETE FROM review_runs
WHERE id = $1 AND user_id = $2
`

type DeleteRunParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

// Discards a run. The snapshot goes with it, but the answers stay in
// review_logs with run_id set to NULL — retention evidence is not something a
// discarded practice run may erase (D-050).
func (q *Queries) DeleteRun(ctx context.Context, arg DeleteRunParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteRun, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const drawRandomCards = `-- name: DrawRandomCards :many

SELECT c.id AS card_id
FROM cards c
WHERE c.user_id = $1
  AND c.deleted_at IS NULL
  -- coalesce, not a bare cardinality(): pgx encodes a nil Go slice as SQL
  -- NULL rather than '{}', and cardinality(NULL) is NULL, which makes the
  -- whole OR null and drops every row. "No filter" would then silently mean
  -- "match nothing" for any caller that passed nil instead of an empty slice.
  AND (coalesce(cardinality($2::uuid[]), 0) = 0
       OR c.domain_id = ANY($2::uuid[]))
  AND (coalesce(cardinality($3::uuid[]), 0) = 0
       OR EXISTS (SELECT 1 FROM card_categories cc
                   WHERE cc.card_id = c.id
                     AND cc.category_id = ANY($3::uuid[])))
ORDER BY random()
LIMIT $4
`

type DrawRandomCardsParams struct {
	UserID      uuid.UUID   `json:"user_id"`
	DomainIds   []uuid.UUID `json:"domain_ids"`
	CategoryIds []uuid.UUID `json:"category_ids"`
	Want        int32       `json:"want"`
}

// The draw, and the questions.
// Eligible cards for a random draw: live, this user's, and inside the set's
// filters. Mastered cards are included on purpose — "do I still know this" is
// the whole point, and a set is not the scheduled queue (D-048).
//
// An empty array means the filter is off, which is how a set with no domains
// and no categories draws from the whole knowledge base. Domains are OR'd
// against each other and so are categories, but the two groups are AND'ed:
// picking Matematika and the "rumus" category means cards that are both.
func (q *Queries) DrawRandomCards(ctx context.Context, arg DrawRandomCardsParams) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, drawRandomCards,
		arg.UserID,
		arg.DomainIds,
		arg.CategoryIds,
		arg.Want,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []uuid.UUID{}
	for rows.Next() {
		var card_id uuid.UUID
		if err := rows.Scan(&card_id); err != nil {
			return nil, err
		}
		items = append(items, card_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const finishRun = `-- name: FinishRun :one
UPDATE review_runs r
SET finished_at   = now(),
    total_count   = (SELECT count(*) FROM review_run_cards rc
                      WHERE rc.run_id = r.id),
    correct_count = (SELECT count(*) FROM review_logs rl
                      WHERE rl.run_id = r.id AND rl.rating = 'ingat')
WHERE r.id = $1 AND r.user_id = $2 AND r.finished_at IS NULL
RETURNING id, set_id, user_id, started_at, finished_at, run_date, total_count, correct_count
`

type FinishRunParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

// The counts are recomputed here rather than accepted from the client. total
// is how many questions the run drew, so an abandoned one scores against
// everything it was asked, not only what it answered.
func (q *Queries) FinishRun(ctx context.Context, arg FinishRunParams) (ReviewRun, error) {
	row := q.db.QueryRow(ctx, finishRun, arg.ID, arg.UserID)
	var i ReviewRun
	err := row.Scan(
		&i.ID,
		&i.SetID,
		&i.UserID,
		&i.StartedAt,
		&i.FinishedAt,
		&i.RunDate,
		&i.TotalCount,
		&i.CorrectCount,
	)
	return i, err
}

const getOpenRun = `-- name: GetOpenRun :one

SELECT id, set_id, user_id, started_at, finished_at, run_date, total_count, correct_count FROM review_runs
WHERE set_id = $1 AND user_id = $2 AND finished_at IS NULL
`

type GetOpenRunParams struct {
	SetID  uuid.UUID `json:"set_id"`
	UserID uuid.UUID `json:"user_id"`
}

// Runs.
// At most one can exist: a partial unique index enforces it.
func (q *Queries) GetOpenRun(ctx context.Context, arg GetOpenRunParams) (ReviewRun, error) {
	row := q.db.QueryRow(ctx, getOpenRun, arg.SetID, arg.UserID)
	var i ReviewRun
	err := row.Scan(
		&i.ID,
		&i.SetID,
		&i.UserID,
		&i.StartedAt,
		&i.FinishedAt,
		&i.RunDate,
		&i.TotalCount,
		&i.CorrectCount,
	)
	return i, err
}

const getReviewSet = `-- name: GetReviewSet :one
SELECT s.id, s.user_id, s.title, s.description, s.selection, s.question_count, s.time_limit_minutes, s.archived_at, s.created_at, s.updated_at, s.format,
       (SELECT count(*) FROM review_runs r
         WHERE r.set_id = s.id AND r.finished_at IS NOT NULL) AS run_count,
       COALESCE((SELECT array_agg(sd.domain_id ORDER BY sd.domain_id)
                   FROM review_set_domains sd WHERE sd.set_id = s.id),
                '{}')::uuid[] AS domain_ids,
       COALESCE((SELECT array_agg(sc.category_id ORDER BY sc.category_id)
                   FROM review_set_categories sc WHERE sc.set_id = s.id),
                '{}')::uuid[] AS category_ids
FROM review_sets s
WHERE s.id = $1 AND s.user_id = $2
`

type GetReviewSetParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

type GetReviewSetRow struct {
	ID               uuid.UUID   `json:"id"`
	UserID           uuid.UUID   `json:"user_id"`
	Title            string      `json:"title"`
	Description      string      `json:"description"`
	Selection        string      `json:"selection"`
	QuestionCount    *int32      `json:"question_count"`
	TimeLimitMinutes *int32      `json:"time_limit_minutes"`
	ArchivedAt       *time.Time  `json:"archived_at"`
	CreatedAt        time.Time   `json:"created_at"`
	UpdatedAt        time.Time   `json:"updated_at"`
	Format           string      `json:"format"`
	RunCount         int64       `json:"run_count"`
	DomainIds        []uuid.UUID `json:"domain_ids"`
	CategoryIds      []uuid.UUID `json:"category_ids"`
}

func (q *Queries) GetReviewSet(ctx context.Context, arg GetReviewSetParams) (GetReviewSetRow, error) {
	row := q.db.QueryRow(ctx, getReviewSet, arg.ID, arg.UserID)
	var i GetReviewSetRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.Selection,
		&i.QuestionCount,
		&i.TimeLimitMinutes,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Format,
		&i.RunCount,
		&i.DomainIds,
		&i.CategoryIds,
	)
	return i, err
}

const getRun = `-- name: GetRun :one
SELECT id, set_id, user_id, started_at, finished_at, run_date, total_count, correct_count FROM review_runs
WHERE id = $1 AND user_id = $2
`

type GetRunParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) GetRun(ctx context.Context, arg GetRunParams) (ReviewRun, error) {
	row := q.db.QueryRow(ctx, getRun, arg.ID, arg.UserID)
	var i ReviewRun
	err := row.Scan(
		&i.ID,
		&i.SetID,
		&i.UserID,
		&i.StartedAt,
		&i.FinishedAt,
		&i.RunDate,
		&i.TotalCount,
		&i.CorrectCount,
	)
	return i, err
}

const getRunQuestion = `-- name: GetRunQuestion :one
SELECT rc.position, rc.card_id, rc.options, rc.correct_index,
       c.front, c.back, c.deleted_at
FROM review_run_cards rc
LEFT JOIN cards c
       ON c.user_id = rc.user_id AND c.id = rc.card_id
WHERE rc.run_id = $1 AND rc.user_id = $2 AND rc.card_id = $3
`

type GetRunQuestionParams struct {
	RunID  uuid.UUID `json:"run_id"`
	UserID uuid.UUID `json:"user_id"`
	CardID uuid.UUID `json:"card_id"`
}

type GetRunQuestionRow struct {
	Position     int32      `json:"position"`
	CardID       uuid.UUID  `json:"card_id"`
	Options      []string   `json:"options"`
	CorrectIndex *int32     `json:"correct_index"`
	Front        *string    `json:"front"`
	Back         *string    `json:"back"`
	DeletedAt    *time.Time `json:"deleted_at"`
}

// One question, with everything the server needs to grade or reveal it. No
// rows means the card is not part of this run, which the handler answers 404 —
// it also replaces the old AttemptHasQuestion existence check, so the grading
// path reads one row instead of the whole question list.
func (q *Queries) GetRunQuestion(ctx context.Context, arg GetRunQuestionParams) (GetRunQuestionRow, error) {
	row := q.db.QueryRow(ctx, getRunQuestion, arg.RunID, arg.UserID, arg.CardID)
	var i GetRunQuestionRow
	err := row.Scan(
		&i.Position,
		&i.CardID,
		&i.Options,
		&i.CorrectIndex,
		&i.Front,
		&i.Back,
		&i.DeletedAt,
	)
	return i, err
}

const insertSetAnswer = `-- name: InsertSetAnswer :exec
INSERT INTO review_logs (card_id, user_id, rating,
                         interval_before, interval_after,
                         source, run_id, format)
VALUES ($1, $2, $3, $5, $5,
        'set', $4, $6)
ON CONFLICT (run_id, card_id)
    WHERE run_id IS NOT NULL
    DO NOTHING
`

type InsertSetAnswerParams struct {
	CardID       uuid.UUID  `json:"card_id"`
	UserID       uuid.UUID  `json:"user_id"`
	Rating       string     `json:"rating"`
	RunID        *uuid.UUID `json:"run_id"`
	IntervalDays int32      `json:"interval_days"`
	Format       string     `json:"format"`
}

// An answer given inside a set is a review that does not move the schedule
// (D-049), so interval_before and interval_after are written equal — the
// ladder did not advance. `format` records whether the user recalled it or
// recognised it among options, so the retention metric can tell the two apart
// (D-077). ON CONFLICT DO NOTHING against the partial unique index makes a
// double-submitted answer idempotent instead of a corrupt score.
func (q *Queries) InsertSetAnswer(ctx context.Context, arg InsertSetAnswerParams) error {
	_, err := q.db.Exec(ctx, insertSetAnswer,
		arg.CardID,
		arg.UserID,
		arg.Rating,
		arg.RunID,
		arg.IntervalDays,
		arg.Format,
	)
	return err
}

const listCardBacks = `-- name: ListCardBacks :many
SELECT id, back FROM cards
WHERE user_id = $1 AND id = ANY($2::uuid[])
  AND deleted_at IS NULL
`

type ListCardBacksParams struct {
	UserID  uuid.UUID   `json:"user_id"`
	CardIds []uuid.UUID `json:"card_ids"`
}

type ListCardBacksRow struct {
	ID   uuid.UUID `json:"id"`
	Back string    `json:"back"`
}

// The answers for the drawn question set, to seed the correct option of each
// multiple-choice question. Soft-deleted cards are excluded: a card deleted
// between the draw and the snapshot has no answer to be right about, and that
// question falls back to recall.
func (q *Queries) ListCardBacks(ctx context.Context, arg ListCardBacksParams) ([]ListCardBacksRow, error) {
	rows, err := q.db.Query(ctx, listCardBacks, arg.UserID, arg.CardIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListCardBacksRow{}
	for rows.Next() {
		var i ListCardBacksRow
		if err := rows.Scan(&i.ID, &i.Back); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listDistractorPool = `-- name: ListDistractorPool :many
SELECT back FROM (
    SELECT DISTINCT c.back
    FROM cards c
    WHERE c.user_id = $1
      AND c.deleted_at IS NULL
      AND c.back <> ''
      -- coalesce for the same reason as DrawRandomCards, and it matters more
      -- here: the widening pass deliberately calls this with no filters at
      -- all, so nil is the normal input rather than an edge case.
      AND (coalesce(cardinality($2::uuid[]), 0) = 0
           OR c.domain_id = ANY($2::uuid[]))
      AND (coalesce(cardinality($3::uuid[]), 0) = 0
           OR EXISTS (SELECT 1 FROM card_categories cc
                       WHERE cc.card_id = c.id
                         AND cc.category_id = ANY($3::uuid[])))
) d
ORDER BY random()
LIMIT $4
`

type ListDistractorPoolParams struct {
	UserID      uuid.UUID   `json:"user_id"`
	DomainIds   []uuid.UUID `json:"domain_ids"`
	CategoryIds []uuid.UUID `json:"category_ids"`
	Want        int32       `json:"want"`
}

// Candidate wrong answers, taken from the backs of the user's other cards
// (D-077). Nothing is authored for this: making the user write three plausible
// wrong answers per card would tax capture, which is the one cost this product
// protects above all (hard rule 7).
//
// One pool for the whole run rather than three fresh draws per question. A
// run of 50 questions would otherwise be 50 extra round trips inside the
// transaction that starts it, and the pool answers the same need: which of
// this user's answers are plausible next to each other. Distractors repeating
// across questions in one run is ordinary for a quiz, not a defect.
//
// Empty backs are excluded because an empty option is not a choice. The
// correct answer is *not* excluded here — it cannot be, one pool serves every
// question — so the caller drops it per question before sampling.
//
// DISTINCT sits in a subquery because ORDER BY random() is not allowed
// alongside SELECT DISTINCT: the sort expression has to appear in the select
// list, and adding it there would defeat the DISTINCT.
func (q *Queries) ListDistractorPool(ctx context.Context, arg ListDistractorPoolParams) ([]string, error) {
	rows, err := q.db.Query(ctx, listDistractorPool,
		arg.UserID,
		arg.DomainIds,
		arg.CategoryIds,
		arg.Want,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var back string
		if err := rows.Scan(&back); err != nil {
			return nil, err
		}
		items = append(items, back)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listReviewSets = `-- name: ListReviewSets :many

SELECT s.id, s.user_id, s.title, s.description, s.selection, s.question_count, s.time_limit_minutes, s.archived_at, s.created_at, s.updated_at, s.format,
       (SELECT count(*) FROM review_runs r
         WHERE r.set_id = s.id AND r.finished_at IS NOT NULL) AS run_count,
       COALESCE((SELECT array_agg(sd.domain_id ORDER BY sd.domain_id)
                   FROM review_set_domains sd WHERE sd.set_id = s.id),
                '{}')::uuid[] AS domain_ids,
       COALESCE((SELECT array_agg(sc.category_id ORDER BY sc.category_id)
                   FROM review_set_categories sc WHERE sc.set_id = s.id),
                '{}')::uuid[] AS category_ids,
       -- How many match before LIMIT, carried on every row — the same shape
       -- ListNotes and ListCards use (D-084). This list had no LIMIT at all
       -- before, so every subquery above ran once per set the account had ever
       -- made, on every load of the Ulangan screen.
       count(*) OVER () AS total
FROM review_sets s
WHERE s.user_id = $1
  AND CASE WHEN $2::bool
           THEN s.archived_at IS NOT NULL
           ELSE s.archived_at IS NULL
      END
ORDER BY s.created_at DESC, s.id DESC
LIMIT $4 OFFSET $3
`

type ListReviewSetsParams struct {
	UserID    uuid.UUID `json:"user_id"`
	Archived  bool      `json:"archived"`
	RowOffset int32     `json:"row_offset"`
	RowLimit  int32     `json:"row_limit"`
}

type ListReviewSetsRow struct {
	ID               uuid.UUID   `json:"id"`
	UserID           uuid.UUID   `json:"user_id"`
	Title            string      `json:"title"`
	Description      string      `json:"description"`
	Selection        string      `json:"selection"`
	QuestionCount    *int32      `json:"question_count"`
	TimeLimitMinutes *int32      `json:"time_limit_minutes"`
	ArchivedAt       *time.Time  `json:"archived_at"`
	CreatedAt        time.Time   `json:"created_at"`
	UpdatedAt        time.Time   `json:"updated_at"`
	Format           string      `json:"format"`
	RunCount         int64       `json:"run_count"`
	DomainIds        []uuid.UUID `json:"domain_ids"`
	CategoryIds      []uuid.UUID `json:"category_ids"`
	Total            int64       `json:"total"`
}

// Review sets: a saved, repeatable configuration for a review over the cards
// that already exist (D-048, renamed by D-075). There is no question bank — a
// second place for knowledge to live is exactly what D-005 collapsed.
//
// A `review_set` is the configuration; a `review_run` is one sitting of it.
// The other path into a review is the scheduled due queue, which needs no
// configuration and lives in cards.sql.
//
// Every query carries user_id, and the composite foreign keys make a
// cross-tenant reference impossible even if one is forgotten (D-047).
// run_count counts finished runs only: an abandoned one is not a score, and a
// list that counted it would tell the user they had sat something they had
// not. The filter arrays are aggregated here rather than fetched per set,
// which is the same shape ListCards uses for its categories.
//
// `archived` switches the whole list to the archive, one query rather than two
// so the aggregation cannot drift between them — same reason ListCards folds
// the Terhapus view in rather than forking.
// id breaks the tie. created_at is not unique, and a page boundary landing
// inside a tie serves one row twice and skips another.
func (q *Queries) ListReviewSets(ctx context.Context, arg ListReviewSetsParams) ([]ListReviewSetsRow, error) {
	rows, err := q.db.Query(ctx, listReviewSets,
		arg.UserID,
		arg.Archived,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListReviewSetsRow{}
	for rows.Next() {
		var i ListReviewSetsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Title,
			&i.Description,
			&i.Selection,
			&i.QuestionCount,
			&i.TimeLimitMinutes,
			&i.ArchivedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Format,
			&i.RunCount,
			&i.DomainIds,
			&i.CategoryIds,
			&i.Total,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRunQuestions = `-- name: ListRunQuestions :many
SELECT rc.position, rc.card_id, rc.options,
       c.front, c.deleted_at,
       rl.rating
FROM review_run_cards rc
LEFT JOIN cards c
       ON c.user_id = rc.user_id AND c.id = rc.card_id
LEFT JOIN review_logs rl
       ON rl.run_id = rc.run_id
      AND rl.card_id = rc.card_id
WHERE rc.run_id = $1 AND rc.user_id = $2
ORDER BY rc.position
`

type ListRunQuestionsParams struct {
	RunID  uuid.UUID `json:"run_id"`
	UserID uuid.UUID `json:"user_id"`
}

type ListRunQuestionsRow struct {
	Position  int32      `json:"position"`
	CardID    uuid.UUID  `json:"card_id"`
	Options   []string   `json:"options"`
	Front     *string    `json:"front"`
	DeletedAt *time.Time `json:"deleted_at"`
	Rating    *string    `json:"rating"`
}

// The run's question set in presentation order, each with the answer already
// given if there is one. This is what makes a run resumable: the unanswered
// questions are the rows where rating is NULL (D-050).
//
// `back` is not selected and `correct_index` is not selected. The first is
// recall before reveal held at the SQL layer, the same way ListDueCards holds
// it (D-003); the second is what keeps a multiple-choice question from
// shipping its own answer key. Both are read one row at a time by
// GetRunQuestion, on the request that grades or reveals.
//
// The join to cards is LEFT: the snapshot deliberately has no foreign key to
// cards so that deleting one cannot erase a finished run's history. A question
// whose card is gone still occupies its position in the score.
func (q *Queries) ListRunQuestions(ctx context.Context, arg ListRunQuestionsParams) ([]ListRunQuestionsRow, error) {
	rows, err := q.db.Query(ctx, listRunQuestions, arg.RunID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRunQuestionsRow{}
	for rows.Next() {
		var i ListRunQuestionsRow
		if err := rows.Scan(
			&i.Position,
			&i.CardID,
			&i.Options,
			&i.Front,
			&i.DeletedAt,
			&i.Rating,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRuns = `-- name: ListRuns :many
SELECT r.id, r.set_id, r.user_id, r.started_at, r.finished_at, r.run_date, r.total_count, r.correct_count, count(*) OVER () AS total
FROM review_runs r
WHERE r.set_id = $1 AND r.user_id = $2 AND r.finished_at IS NOT NULL
ORDER BY r.started_at DESC, r.id DESC
LIMIT $4 OFFSET $3
`

type ListRunsParams struct {
	SetID     uuid.UUID `json:"set_id"`
	UserID    uuid.UUID `json:"user_id"`
	RowOffset int32     `json:"row_offset"`
	RowLimit  int32     `json:"row_limit"`
}

type ListRunsRow struct {
	ReviewRun ReviewRun `json:"review_run"`
	Total     int64     `json:"total"`
}

// The history of a set: finished sittings only, newest first, one page at a
// time.
//
// It used to return every run up to a hardcoded twenty with no OFFSET, so the
// twenty-first sitting of a set existed, counted in run_count, appeared in the
// export, and could not be reached by any request the API was able to express
// — the same failure D-084 fixed for notes and cards.
//
// Finished only, because the open run is not history: it is the thing the
// Mulai button resumes, and the detail response carries it separately through
// GetOpenRun. Folding it into a paged list would make "is there one open"
// depend on which page you happened to be looking at. It also makes `total`
// here exactly the run_count the set carries, which counts finished runs for
// the same reason.
// id breaks the tie, as in every other paged list.
func (q *Queries) ListRuns(ctx context.Context, arg ListRunsParams) ([]ListRunsRow, error) {
	rows, err := q.db.Query(ctx, listRuns,
		arg.SetID,
		arg.UserID,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRunsRow{}
	for rows.Next() {
		var i ListRunsRow
		if err := rows.Scan(
			&i.ReviewRun.ID,
			&i.ReviewRun.SetID,
			&i.ReviewRun.UserID,
			&i.ReviewRun.StartedAt,
			&i.ReviewRun.FinishedAt,
			&i.ReviewRun.RunDate,
			&i.ReviewRun.TotalCount,
			&i.ReviewRun.CorrectCount,
			&i.Total,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSetCards = `-- name: ListSetCards :many

SELECT sc.card_id, sc.position, c.front
FROM review_set_cards sc
JOIN cards c ON c.user_id = sc.user_id AND c.id = sc.card_id
WHERE sc.set_id = $1 AND sc.user_id = $2 AND c.deleted_at IS NULL
ORDER BY sc.position
`

type ListSetCardsParams struct {
	SetID  uuid.UUID `json:"set_id"`
	UserID uuid.UUID `json:"user_id"`
}

type ListSetCardsRow struct {
	CardID   uuid.UUID `json:"card_id"`
	Position int32     `json:"position"`
	Front    string    `json:"front"`
}

// The pinned question set, selection = 'fixed' only.
func (q *Queries) ListSetCards(ctx context.Context, arg ListSetCardsParams) ([]ListSetCardsRow, error) {
	rows, err := q.db.Query(ctx, listSetCards, arg.SetID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSetCardsRow{}
	for rows.Next() {
		var i ListSetCardsRow
		if err := rows.Scan(&i.CardID, &i.Position, &i.Front); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const snapshotRunCard = `-- name: SnapshotRunCard :exec
INSERT INTO review_run_cards (run_id, user_id, card_id, position,
                              options, correct_index)
VALUES ($1, $2, $3, $4, $5::text[], $6)
`

type SnapshotRunCardParams struct {
	RunID        uuid.UUID `json:"run_id"`
	UserID       uuid.UUID `json:"user_id"`
	CardID       uuid.UUID `json:"card_id"`
	Position     int32     `json:"position"`
	Options      []string  `json:"options"`
	CorrectIndex *int32    `json:"correct_index"`
}

// options and correct_index are NULL for a recall question, and for a choice
// question that could not find enough distractors to build from.
func (q *Queries) SnapshotRunCard(ctx context.Context, arg SnapshotRunCardParams) error {
	_, err := q.db.Exec(ctx, snapshotRunCard,
		arg.RunID,
		arg.UserID,
		arg.CardID,
		arg.Position,
		arg.Options,
		arg.CorrectIndex,
	)
	return err
}

const unarchiveReviewSet = `-- name: UnarchiveReviewSet :one
UPDATE review_sets
SET archived_at = NULL, updated_at = now()
WHERE id = $1 AND user_id = $2 AND archived_at IS NOT NULL
RETURNING id, user_id, title, description, selection, question_count, time_limit_minutes, archived_at, created_at, updated_at, format
`

type UnarchiveReviewSetParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

// Exams had no way back, unlike domains and categories. Archiving is meant to
// be the safe alternative to deleting, and a safe alternative you cannot undo
// is only half of one.
func (q *Queries) UnarchiveReviewSet(ctx context.Context, arg UnarchiveReviewSetParams) (ReviewSet, error) {
	row := q.db.QueryRow(ctx, unarchiveReviewSet, arg.ID, arg.UserID)
	var i ReviewSet
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.Selection,
		&i.QuestionCount,
		&i.TimeLimitMinutes,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Format,
	)
	return i, err
}

const updateReviewSet = `-- name: UpdateReviewSet :one
UPDATE review_sets
SET title              = $3,
    description        = $4,
    selection          = $5,
    question_count     = $6,
    time_limit_minutes = $7,
    format             = $8,
    updated_at         = now()
WHERE id = $1 AND user_id = $2
RETURNING id, user_id, title, description, selection, question_count, time_limit_minutes, archived_at, created_at, updated_at, format
`

type UpdateReviewSetParams struct {
	ID               uuid.UUID `json:"id"`
	UserID           uuid.UUID `json:"user_id"`
	Title            string    `json:"title"`
	Description      string    `json:"description"`
	Selection        string    `json:"selection"`
	QuestionCount    *int32    `json:"question_count"`
	TimeLimitMinutes *int32    `json:"time_limit_minutes"`
	Format           string    `json:"format"`
}

func (q *Queries) UpdateReviewSet(ctx context.Context, arg UpdateReviewSetParams) (ReviewSet, error) {
	row := q.db.QueryRow(ctx, updateReviewSet,
		arg.ID,
		arg.UserID,
		arg.Title,
		arg.Description,
		arg.Selection,
		arg.QuestionCount,
		arg.TimeLimitMinutes,
		arg.Format,
	)
	var i ReviewSet
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Title,
		&i.Description,
		&i.Selection,
		&i.QuestionCount,
		&i.TimeLimitMinutes,
		&i.ArchivedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Format,
	)
	return i, err
}
