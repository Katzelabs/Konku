// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: export.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const exportCardCategories = `-- name: ExportCardCategories :many
SELECT card_id, category_id
FROM card_categories
WHERE user_id = $1
`

type ExportCardCategoriesRow struct {
	CardID     uuid.UUID `json:"card_id"`
	CategoryID uuid.UUID `json:"category_id"`
}

func (q *Queries) ExportCardCategories(ctx context.Context, userID uuid.UUID) ([]ExportCardCategoriesRow, error) {
	rows, err := q.db.Query(ctx, exportCardCategories, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCardCategoriesRow{}
	for rows.Next() {
		var i ExportCardCategoriesRow
		if err := rows.Scan(&i.CardID, &i.CategoryID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportCardSchedules = `-- name: ExportCardSchedules :many
SELECT card_id, stage, next_review_date, lapses, state
FROM card_schedules
WHERE user_id = $1
`

type ExportCardSchedulesRow struct {
	CardID         uuid.UUID  `json:"card_id"`
	Stage          int32      `json:"stage"`
	NextReviewDate *time.Time `json:"next_review_date"`
	Lapses         int32      `json:"lapses"`
	State          string     `json:"state"`
}

// The part that cannot be reconstructed from the notes: where each card sits
// in the rotation.
func (q *Queries) ExportCardSchedules(ctx context.Context, userID uuid.UUID) ([]ExportCardSchedulesRow, error) {
	rows, err := q.db.Query(ctx, exportCardSchedules, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCardSchedulesRow{}
	for rows.Next() {
		var i ExportCardSchedulesRow
		if err := rows.Scan(
			&i.CardID,
			&i.Stage,
			&i.NextReviewDate,
			&i.Lapses,
			&i.State,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportCards = `-- name: ExportCards :many
SELECT id, type, front, back, domain_id, deleted_at, created_at, updated_at
FROM cards
WHERE user_id = $1
ORDER BY created_at
`

type ExportCardsRow struct {
	ID        uuid.UUID  `json:"id"`
	Type      string     `json:"type"`
	Front     string     `json:"front"`
	Back      string     `json:"back"`
	DomainID  *uuid.UUID `json:"domain_id"`
	DeletedAt *time.Time `json:"deleted_at"`
	CreatedAt time.Time  `json:"created_at"`
	UpdatedAt time.Time  `json:"updated_at"`
}

func (q *Queries) ExportCards(ctx context.Context, userID uuid.UUID) ([]ExportCardsRow, error) {
	rows, err := q.db.Query(ctx, exportCards, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCardsRow{}
	for rows.Next() {
		var i ExportCardsRow
		if err := rows.Scan(
			&i.ID,
			&i.Type,
			&i.Front,
			&i.Back,
			&i.DomainID,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportCategories = `-- name: ExportCategories :many
SELECT id, slug, label, color, archived_at, created_at
FROM categories
WHERE user_id = $1
ORDER BY created_at
`

type ExportCategoriesRow struct {
	ID         uuid.UUID  `json:"id"`
	Slug       string     `json:"slug"`
	Label      string     `json:"label"`
	Color      string     `json:"color"`
	ArchivedAt *time.Time `json:"archived_at"`
	CreatedAt  time.Time  `json:"created_at"`
}

// Colour travels with the archive (00011). It is a thing the user chose, and
// an export that silently drops the choices they made is not the whole account.
func (q *Queries) ExportCategories(ctx context.Context, userID uuid.UUID) ([]ExportCategoriesRow, error) {
	rows, err := q.db.Query(ctx, exportCategories, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportCategoriesRow{}
	for rows.Next() {
		var i ExportCategoriesRow
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Label,
			&i.Color,
			&i.ArchivedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportDomains = `-- name: ExportDomains :many
SELECT id, slug, label, color, weekly_quota, sort_order, archived_at, created_at
FROM domains
WHERE user_id = $1
ORDER BY sort_order, created_at
`

type ExportDomainsRow struct {
	ID          uuid.UUID  `json:"id"`
	Slug        string     `json:"slug"`
	Label       string     `json:"label"`
	Color       string     `json:"color"`
	WeeklyQuota int32      `json:"weekly_quota"`
	SortOrder   int32      `json:"sort_order"`
	ArchivedAt  *time.Time `json:"archived_at"`
	CreatedAt   time.Time  `json:"created_at"`
}

func (q *Queries) ExportDomains(ctx context.Context, userID uuid.UUID) ([]ExportDomainsRow, error) {
	rows, err := q.db.Query(ctx, exportDomains, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportDomainsRow{}
	for rows.Next() {
		var i ExportDomainsRow
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Label,
			&i.Color,
			&i.WeeklyQuota,
			&i.SortOrder,
			&i.ArchivedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportFocusSessions = `-- name: ExportFocusSessions :many
SELECT id, domain_id, duration_minutes, session_date, completed_at
FROM focus_sessions
WHERE user_id = $1
ORDER BY session_date, completed_at
`

type ExportFocusSessionsRow struct {
	ID              uuid.UUID  `json:"id"`
	DomainID        *uuid.UUID `json:"domain_id"`
	DurationMinutes int32      `json:"duration_minutes"`
	SessionDate     time.Time  `json:"session_date"`
	CompletedAt     time.Time  `json:"completed_at"`
}

func (q *Queries) ExportFocusSessions(ctx context.Context, userID uuid.UUID) ([]ExportFocusSessionsRow, error) {
	rows, err := q.db.Query(ctx, exportFocusSessions, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportFocusSessionsRow{}
	for rows.Next() {
		var i ExportFocusSessionsRow
		if err := rows.Scan(
			&i.ID,
			&i.DomainID,
			&i.DurationMinutes,
			&i.SessionDate,
			&i.CompletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportNoteCategories = `-- name: ExportNoteCategories :many
SELECT note_id, category_id
FROM note_categories
WHERE user_id = $1
`

type ExportNoteCategoriesRow struct {
	NoteID     uuid.UUID `json:"note_id"`
	CategoryID uuid.UUID `json:"category_id"`
}

func (q *Queries) ExportNoteCategories(ctx context.Context, userID uuid.UUID) ([]ExportNoteCategoriesRow, error) {
	rows, err := q.db.Query(ctx, exportNoteCategories, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportNoteCategoriesRow{}
	for rows.Next() {
		var i ExportNoteCategoriesRow
		if err := rows.Scan(&i.NoteID, &i.CategoryID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportNotes = `-- name: ExportNotes :many
SELECT id, title, content_md, domain_id, deleted_at, created_at, updated_at
FROM notes
WHERE user_id = $1
ORDER BY created_at
`

type ExportNotesRow struct {
	ID        uuid.UUID  `json:"id"`
	Title     string     `json:"title"`
	ContentMd string     `json:"content_md"`
	DomainID  *uuid.UUID `json:"domain_id"`
	DeletedAt *time.Time `json:"deleted_at"`
	CreatedAt time.Time  `json:"created_at"`
	UpdatedAt time.Time  `json:"updated_at"`
}

func (q *Queries) ExportNotes(ctx context.Context, userID uuid.UUID) ([]ExportNotesRow, error) {
	rows, err := q.db.Query(ctx, exportNotes, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportNotesRow{}
	for rows.Next() {
		var i ExportNotesRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.ContentMd,
			&i.DomainID,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewLogs = `-- name: ExportReviewLogs :many
SELECT id, card_id, rating, interval_before, interval_after, reviewed_at,
       source, run_id, format
FROM review_logs
WHERE user_id = $1
ORDER BY reviewed_at
`

type ExportReviewLogsRow struct {
	ID             int64      `json:"id"`
	CardID         uuid.UUID  `json:"card_id"`
	Rating         string     `json:"rating"`
	IntervalBefore int32      `json:"interval_before"`
	IntervalAfter  int32      `json:"interval_after"`
	ReviewedAt     time.Time  `json:"reviewed_at"`
	Source         string     `json:"source"`
	RunID          *uuid.UUID `json:"run_id"`
	Format         string     `json:"format"`
}

// The retention history, which is the one dataset here that cannot be
// recreated after the fact (D-029). Oldest first, so the file reads as a
// timeline.
func (q *Queries) ExportReviewLogs(ctx context.Context, userID uuid.UUID) ([]ExportReviewLogsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewLogs, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewLogsRow{}
	for rows.Next() {
		var i ExportReviewLogsRow
		if err := rows.Scan(
			&i.ID,
			&i.CardID,
			&i.Rating,
			&i.IntervalBefore,
			&i.IntervalAfter,
			&i.ReviewedAt,
			&i.Source,
			&i.RunID,
			&i.Format,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewRunCards = `-- name: ExportReviewRunCards :many
SELECT run_id, card_id, position, options, correct_index
FROM review_run_cards
WHERE user_id = $1
ORDER BY run_id, position
`

type ExportReviewRunCardsRow struct {
	RunID        uuid.UUID `json:"run_id"`
	CardID       uuid.UUID `json:"card_id"`
	Position     int32     `json:"position"`
	Options      []string  `json:"options"`
	CorrectIndex *int32    `json:"correct_index"`
}

// options and correct_index included: they are what the question actually
// looked like, and a run's history without them is not the run.
func (q *Queries) ExportReviewRunCards(ctx context.Context, userID uuid.UUID) ([]ExportReviewRunCardsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewRunCards, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewRunCardsRow{}
	for rows.Next() {
		var i ExportReviewRunCardsRow
		if err := rows.Scan(
			&i.RunID,
			&i.CardID,
			&i.Position,
			&i.Options,
			&i.CorrectIndex,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewRuns = `-- name: ExportReviewRuns :many
SELECT id, set_id, started_at, finished_at, run_date, total_count,
       correct_count
FROM review_runs
WHERE user_id = $1
ORDER BY started_at
`

type ExportReviewRunsRow struct {
	ID           uuid.UUID  `json:"id"`
	SetID        uuid.UUID  `json:"set_id"`
	StartedAt    time.Time  `json:"started_at"`
	FinishedAt   *time.Time `json:"finished_at"`
	RunDate      time.Time  `json:"run_date"`
	TotalCount   int32      `json:"total_count"`
	CorrectCount int32      `json:"correct_count"`
}

func (q *Queries) ExportReviewRuns(ctx context.Context, userID uuid.UUID) ([]ExportReviewRunsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewRuns, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewRunsRow{}
	for rows.Next() {
		var i ExportReviewRunsRow
		if err := rows.Scan(
			&i.ID,
			&i.SetID,
			&i.StartedAt,
			&i.FinishedAt,
			&i.RunDate,
			&i.TotalCount,
			&i.CorrectCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewSetCards = `-- name: ExportReviewSetCards :many
SELECT set_id, card_id, position
FROM review_set_cards
WHERE user_id = $1
ORDER BY set_id, position
`

type ExportReviewSetCardsRow struct {
	SetID    uuid.UUID `json:"set_id"`
	CardID   uuid.UUID `json:"card_id"`
	Position int32     `json:"position"`
}

func (q *Queries) ExportReviewSetCards(ctx context.Context, userID uuid.UUID) ([]ExportReviewSetCardsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewSetCards, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewSetCardsRow{}
	for rows.Next() {
		var i ExportReviewSetCardsRow
		if err := rows.Scan(&i.SetID, &i.CardID, &i.Position); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewSetCategories = `-- name: ExportReviewSetCategories :many
SELECT set_id, category_id
FROM review_set_categories
WHERE user_id = $1
ORDER BY set_id, category_id
`

type ExportReviewSetCategoriesRow struct {
	SetID      uuid.UUID `json:"set_id"`
	CategoryID uuid.UUID `json:"category_id"`
}

func (q *Queries) ExportReviewSetCategories(ctx context.Context, userID uuid.UUID) ([]ExportReviewSetCategoriesRow, error) {
	rows, err := q.db.Query(ctx, exportReviewSetCategories, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewSetCategoriesRow{}
	for rows.Next() {
		var i ExportReviewSetCategoriesRow
		if err := rows.Scan(&i.SetID, &i.CategoryID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewSetDomains = `-- name: ExportReviewSetDomains :many
SELECT set_id, domain_id
FROM review_set_domains
WHERE user_id = $1
ORDER BY set_id, domain_id
`

type ExportReviewSetDomainsRow struct {
	SetID    uuid.UUID `json:"set_id"`
	DomainID uuid.UUID `json:"domain_id"`
}

// The filters are part of the configuration, so an export that omitted them
// would describe a set that draws from everything (07 L6: the archive is the
// whole account, not a summary of it).
func (q *Queries) ExportReviewSetDomains(ctx context.Context, userID uuid.UUID) ([]ExportReviewSetDomainsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewSetDomains, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewSetDomainsRow{}
	for rows.Next() {
		var i ExportReviewSetDomainsRow
		if err := rows.Scan(&i.SetID, &i.DomainID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportReviewSets = `-- name: ExportReviewSets :many
SELECT id, title, description, selection, question_count,
       time_limit_minutes, format, archived_at, created_at, updated_at
FROM review_sets
WHERE user_id = $1
ORDER BY created_at
`

type ExportReviewSetsRow struct {
	ID               uuid.UUID  `json:"id"`
	Title            string     `json:"title"`
	Description      string     `json:"description"`
	Selection        string     `json:"selection"`
	QuestionCount    *int32     `json:"question_count"`
	TimeLimitMinutes *int32     `json:"time_limit_minutes"`
	Format           string     `json:"format"`
	ArchivedAt       *time.Time `json:"archived_at"`
	CreatedAt        time.Time  `json:"created_at"`
	UpdatedAt        time.Time  `json:"updated_at"`
}

func (q *Queries) ExportReviewSets(ctx context.Context, userID uuid.UUID) ([]ExportReviewSetsRow, error) {
	rows, err := q.db.Query(ctx, exportReviewSets, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExportReviewSetsRow{}
	for rows.Next() {
		var i ExportReviewSetsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.Description,
			&i.Selection,
			&i.QuestionCount,
			&i.TimeLimitMinutes,
			&i.Format,
			&i.ArchivedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const exportUser = `-- name: ExportUser :one

SELECT id, email, first_name, last_name, created_at, email_verified_at
FROM users
WHERE id = $1
`

type ExportUserRow struct {
	ID              uuid.UUID  `json:"id"`
	Email           string     `json:"email"`
	FirstName       string     `json:"first_name"`
	LastName        string     `json:"last_name"`
	CreatedAt       time.Time  `json:"created_at"`
	EmailVerifiedAt *time.Time `json:"email_verified_at"`
}

// Everything an account owns (07 L6).
//
// Three rules run through this file:
//
//  1. **Explicit columns, never `SELECT *`.** notes.tsv is a generated
//     tsvector that means nothing outside Postgres, and users.password_hash
//     must not leave the server under any circumstance. A `*` here would put
//     both in the archive the first time someone adds a column.
//  2. **No filters beyond the tenancy predicate.** Soft-deleted notes and
//     cards are still rows the account owns, and an export that quietly drops
//     them is the silent disappearance this whole product exists to prevent.
//     The archive separates them into their own folder instead.
//  3. **Every query is scoped by user_id in the WHERE clause** (hard rule 4).
//     RLS backs this up; it does not replace it.
//
// auth_sessions and auth_tokens are deliberately absent. A session id is a
// live credential and a token hash is the shadow of one — neither is content
// the user wrote, and putting them in a file that gets emailed around is a way
// to lose an account, not a way to own your data.
// The name is part of what the account holds about the person, so it is part
// of what "everything we have on you" means (07 L6). Omitting it would make
// the archive quietly incomplete in exactly the field a reader would check
// first.
func (q *Queries) ExportUser(ctx context.Context, id uuid.UUID) (ExportUserRow, error) {
	row := q.db.QueryRow(ctx, exportUser, id)
	var i ExportUserRow
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.FirstName,
		&i.LastName,
		&i.CreatedAt,
		&i.EmailVerifiedAt,
	)
	return i, err
}

const exportUserSettings = `-- name: ExportUserSettings :one
SELECT user_id, default_duration_minutes, focus_step_n, rota_enabled,
       created_at, updated_at
FROM user_settings
WHERE user_id = $1
`

func (q *Queries) ExportUserSettings(ctx context.Context, userID uuid.UUID) (UserSetting, error) {
	row := q.db.QueryRow(ctx, exportUserSettings, userID)
	var i UserSetting
	err := row.Scan(
		&i.UserID,
		&i.DefaultDurationMinutes,
		&i.FocusStepN,
		&i.RotaEnabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
